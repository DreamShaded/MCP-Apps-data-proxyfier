# 06 — add_to_cart (из грида и деталки) + view_cart

Status: code-complete — awaiting HITL verification in Claude Desktop
Type: HITL

## Parent

PRD: `.scratch/mcp-app-proxyfier/PRD.md`

## What to build

Корзина = **реальный сайт**. `add_to_cart(id)` — это **app-initiated tools/call**, который через Playwright выполняет живой клик «в корзину» на megamarket.ru под залогиненной сессией (повторяет естественный user-flow, мягче для антибота, чем bulk). Кнопка доступна **и из выдачи, и из деталки**; во время клика карточка показывает маленький лоадер (live-write). `view_cart` возвращает состояние реальной корзины (товары, количество, сумма) → вью корзины в том же App. Действия сериализуются мьютексом.

## Дизайн

Использует дизайн-систему Megamarket из среза 04 (`megamarket/design-tokens`), снятую с **живого сайта**; вью корзины и бейдж корзины должны визуально соответствовать настоящей корзине Megamarket (строки позиций, количество, сумма, итого). Адаптация под узкий чат-iframe по факту (список позиций, компактные строки), `useAutoResize`, без внешних шрифтов.

## Acceptance criteria

- [x] Вью корзины и бейдж визуально соответствуют настоящей корзине Megamarket, адаптированы под чат-iframe — `cart-view.tsx` + `cart-badge.tsx` + `styles.css` (строки позиций, цена×кол-во, итог, бейдж со счётчиком); финальная визуальная сверка — HITL

- [x] `add_to_cart` выполняет реальный клик «в корзину» под сессией — `scrape-cart.ts` `addToCart()` (переход на товар → клик кнопки → чтение `/cart`); реальное появление в корзине — HITL
- [x] Кнопка работает из грид-вью и из вью деталки — `product-card.tsx` «В корзину» + `product-detail-view.tsx` CTA, обе через `add_to_cart`
- [x] Во время live-добавления показывается индикатор загрузки в карточке — спиннер `.mm-spinner` в кнопке (`addingId`)
- [x] `view_cart` возвращает актуальное состояние реальной корзины — `view-cart-tool.ts` живое чтение `/cart` (без кэша, корзина изменчива)
- [x] Вью корзины рендерится в том же App; бейдж корзины обновляется — `megamarket-app.tsx` (вью `cart`, `setCart` после `add_to_cart`)
- [x] Операции сериализуются (нет параллельных кликов) — вся последовательность в одном `driver.withPage` (мьютекс единственной страницы)
- [ ] **HITL:** в Claude Desktop добавление из обоих мест и просмотр корзины работают — требует ручной сборки и проверки
- [x] Тест слоёв инструментов `add_to_cart`/`view_cart` через шов с фейковым драйвером — `add-to-cart-tool.test.ts` (4 кейса) + `view-cart-tool.test.ts` (3 кейса) + `scrape-cart.test.ts` (6 кейсов)

## Blocked by

- 05
