import type { SkillDefinition } from "../skills/skill-registry.js";

/**
 * Ресурс-скилл `shopping-advisor`: методичка подбора товара, которую агент читает ДО
 * вызова `search_products_advised`. Это четвёртая ступень демо: инструмент и виджет те
 * же, что на третьей, — меняется только то, что агент знает процедуру домена.
 */
export const SHOPPING_ADVISOR_SKILL_URI = "skill://shopping-advisor/SKILL.md";

/**
 * Тело скилла. Опирается только на реальный инструментарий репозитория: поиск с
 * фильтрами `priceMin`/`priceMax`, `get_product(id)`, `add_to_cart`, `checkout`, и на
 * реальные поля DTO (`product.ts`) — `price`, `oldPrice`, `discountPercent`, `rating`,
 * `reviewCount`, `brand`. Ничего сверх этого агенту обещать нельзя.
 */
const SHOPPING_ADVISOR_SKILL_MD = `<!-- ${SHOPPING_ADVISOR_SKILL_URI} -->
---
name: shopping-advisor
description: Подбор товара под задачу покупателя. Читать до вызова search_products_advised.
---

## Порядок
1. Сначала пойми задачу, а не запрос. «Найди наушники» — это не требования. Спроси
   бюджет и сценарий использования (спорт / дорога / звонки / дом), и только если
   пользователь уже назвал их — не переспрашивай.
2. **Есть срок — сначала календарь.** Если в запросе срок назван словами («завтра», «до
   пятницы», «к выходным», «на этой неделе»), вызови get_delivery_calendar ДО поиска.
   Своих часов у тебя нет: сегодняшнюю дату и дату ближайшей пятницы ты можешь узнать
   только оттуда. Не подставляй дату по памяти — она будет из даты обучения.
3. Разреши срок в дату по \`upcoming\` и передай её в поиск:
   search_products_advised(query, filters: { deliveryBy: "YYYY-MM-DD" }). Фильтрует сервер —
   не отбирай по \`delivery\` глазами и не показывай то, что не успевает.
4. Бюджет переводи в фильтры: search_products_advised(query, filters: { priceMin, priceMax }).
   Не фильтруй молча по своей догадке о «нормальной» цене — фильтр показывай в ответе.
5. Отбирай по совокупности \`price\`, \`rating\` и \`reviewCount\`. Рейтинг без объёма отзывов
   ничего не значит: 5.0 при трёх отзывах слабее 4.6 при тысяче. Если \`rating\` или
   \`reviewCount\` равны \`null\` — так и скажи, не додумывай.
6. \`discountPercent\` и \`oldPrice\` — это витрина, а не аргумент. Сравнивай по \`price\`;
   большая скидка от завышенной \`oldPrice\` не делает товар выгодным.
7. Предлагай 2–3 варианта с разным компромиссом (дешевле / баланс / дороже), а не один
   «лучший». Для каждого назови, чем платит покупатель за разницу в цене.
8. Нужны характеристики или комплектация — вызови get_product(id) и опирайся на \`specs\`,
   а не на догадки по названию.
9. Называя срок, бери \`delivery.label\` товара, а не пересказывай своими словами.

## Чего не делать
- Не рекомендовать по максимальной цене и не считать дорогое лучшим по умолчанию.
- Не выдумывать характеристики, которых нет в \`specs\`: каталог статический, чего нет — того нет.
- Не молчать о пустой выдаче: если фильтры ничего не нашли, скажи об этом и предложи
  расширить бюджет или сдвинуть срок, а не подменяй запрос.
- Не считать \`anc: null\` за «шумоподавления нет»: это «характеристика не указана».
- Не обещать срок, которого нет в \`delivery\`, и не называть дату, не сверившись с календарём.
`;

/** Методичка подбора товара — регистрируется общим реестром скиллов. */
export const SHOPPING_ADVISOR_SKILL: SkillDefinition = {
  name: "shopping-advisor",
  description: "Подбор товара под задачу покупателя — читать до вызова search_products_advised.",
  uri: SHOPPING_ADVISOR_SKILL_URI,
  body: SHOPPING_ADVISOR_SKILL_MD,
};
